
    <script src="https://kit.fontawesome.com/2ff29df84d.js" crossorigin="anonymous"></script>
        <h1>User Info</h1>

<!--        <tr>
            <td>
              <strong>name</strong>
              <%= @users.name %>
            </td>
        </tr>

        <tr>
            <td>
              <strong>introduction</strong>
              <%= @users.introduction %>
            </td>
        </tr>
-->
        <h1>New Book</h1>
            <p>Title</p>
            <%= form_with model: @users do |f| %>
            <%= f.text_field :title %>

            <p>Opinion</p>
            <%= f.text_area :body %>
            <% end %>

            <%= link_to 'Create Book', edit_book_path %>