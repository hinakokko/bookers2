

    <tbody>
        <h1>User Info</h1>
                <strong>name</strong>
                <%= @users.name %>
                <strong>introduction</strong>
                <%= @users.introduction %>

        <h1>New Book</h1>
            <p>Title</p>
            <%= form_with model: @users do |f| %>
            <%= f.text_field :title %>

            <p>Opinion</p>
            <%= f.text_area :body %>
            <% end %>

            <%= link_to 'Create Book', book_path %>

    </tbody>