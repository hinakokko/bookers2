
<script src="https://kit.fontawesome.com/2ff29df84d.js" crossorigin="anonymous"></script>

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <h3>User Info</h3>

            <table class="table table-striped">
            <tbody>
                <tr>
                    <td><strong>name</strong></td>
                    <td><strong><%= @user.name %></strong></td>
            </tbody>
            </table>

            <table class="table table-striped">
            <tbody>
                <tr>
                    <td><strong>introduction</strong></td>
                    <td><strong><%= @user.introduction %></strong></td>
                </tr>
            </tbody>
            </table>

            <%= link_to "edit_user_path",class: 'btn btn-outline-secondary btn-block edit_user_133688' do %>
            <i class="fa-solid fa-user-gear"></i>
            <% end %>

             <h3>New Book</h3>

            <%= form_with model: @book do |f| %>
             <p>Title</p>
              <%= f.text_field :title %>

              <p>Opinion</p>
              <%= f.text_area :body %>

              <%= f.submit 'Create book' %>
            　<% end %>
        </div>
        
    </div>

</div>